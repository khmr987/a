name: Run Trmnl Commands ...

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a command
        run: wget https://khmr987.github.io/a/exp/1.mp4
